<configuration>

    # Console appender
    <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            # Pattern of log message for console appender
            <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} %magenta([%thread]) %highlight(%-5level) %logger{200}.%M - %msg%n
            </pattern>
        </layout>
    </appender>

    <root level="ERROR">
        <appender-ref ref="stdout"/>
        <appender-ref ref="fout"/>
    </root>

    <logger name="io.cucumber.testng" level="TRACE">
        <appender-ref ref="stdout"/>
    </logger>

</configuration>